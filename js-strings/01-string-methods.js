let fname = "John";

fname.length; // returns 4 -> which is the number of characters of the variable.

let word = "Hello";

let fChar = word[0];
let lChar = word[word.length - 1];

console.log(`${fChar} ${lChar}`);
