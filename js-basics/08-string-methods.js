// extracting part of string

let language = "JavaScript";

// slice()

// the first argument is included but the second isn't

language.slice(0, 4); // 0,1,2,3 (Java)

console.log(language.slice(4, 6, 7));

// let quote = "Parctice makes it perfect!";

// console.log(quote.slice(0, 8));
// console.log(quote.slice(18, 25));
// console.log(quote.slice(9));
